'use client';
import { Menu, X } from 'lucide-react';
import { useState } from 'react';
export default function SiteHeader(){const [open,setOpen]=useState(false); const links=[['Services','/services'],['Industries','/industries'],['Portfolio','/portfolio'],['Pricing','/pricing'],['About','/about']]; return <header className="fixed inset-x-0 top-0 z-50 border-b border-slate-200/70 bg-white/85 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><a href="/" className="flex items-center gap-2 text-2xl font-black tracking-tight"><span className="brand-mark">R</span>Rak<span className="gradient-text">Vexa</span></a><nav className="hidden items-center gap-7 text-sm font-semibold text-slate-600 md:flex">{links.map(([t,h])=><a key={t} className="hover:text-slate-950" href={h}>{t}</a>)}<a href="/contact" className="rounded-full bg-slate-950 px-5 py-2.5 text-white">Get Free Audit</a></nav><button aria-label="Toggle menu" className="rounded-xl border p-2 md:hidden" onClick={()=>setOpen(!open)}>{open?<X size={20}/>:<Menu size={20}/>}</button></div>{open&&<nav className="border-t bg-white px-5 py-5 md:hidden"><div className="flex flex-col gap-4 font-semibold">{[...links,['Contact','/contact']].map(([t,h])=><a onClick={()=>setOpen(false)} key={t} href={h}>{t}</a>)}</div></nav>}</header>}
